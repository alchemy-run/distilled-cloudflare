{
  "name": "distilled-cloudflare",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "check:write": "biome check --write",
    "check": "biome check",
    "download:env": "doppler secrets download --no-file --format env > .env",
    "find": "bun scripts/find-errors/index.ts",
    "generate:service": "bun scripts/generate-clients.ts --service",
    "generate": "bun scripts/generate-clients.ts",
    "test:watch": "vitest watch",
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "effect": "latest",
    "@effect/platform": "latest"
  },
  "devDependencies": {
    "@effect/cli": "latest",
    "@effect/platform-node": "latest",
    "@effect/vitest": "latest",
    "@types/bun": "latest",
    "vitest": "latest"
  },
  "exports": {
    "./*": {
      "bun": "./src/services/*.ts"
    },
    "./Auth": {
      "bun": "./src/auth.ts"
    },
    "./Category": {
      "bun": "./src/category.ts"
    },
    "./Errors": {
      "bun": "./src/errors.ts"
    },
    "./Traits": {
      "bun": "./src/traits.ts"
    },
    ".": {
      "bun": "./src/index.ts"
    }
  }
}
